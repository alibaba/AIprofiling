# PyKi (Python Kit)

Vendored under AIProf for Python-process profiling (CPython struct extraction, stack/frame decoding).

## Build

<!-- markdownlint-disable commands-show-output -->

```shell
$ pip install -r requirements.txt

$ make reinstall
```
